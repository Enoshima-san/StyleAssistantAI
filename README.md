# StyleAssistantAI
A simple study term project for Computer Science

StyleAssistantAI - это интеллектуальный онлайн-сервис, который подбирает одежду в соответствии с вашими вкусами. Система анализирует ваши параметры, такие как рост, размер и стиль, и, используя искусственный интеллект, предлагает подходящие образы из каталога.

## 🌟 Основные функции

1. Создание аккаунта - зарегистрируйтесь и войдите в систему
2. Настройка профиля - укажите свои персональные данные и предпочтения
3. Умные рекомендации - искусственный интеллект подбирает одежду, учитывая ваши особенности
4. Каталог образов - просматривайте готовые комплекты
5. Избранное - сохраняйте образы, которые вам понравились
6. Лайки/Дизлайки - оценивайте образы, чтобы улучшить персональные рекомендации.

## 🛠️ Для работы проекта требуются:
### Серверная часть (backend):
- Node.js
- Express
- SQLite3
- bcrypt
- jsonwebtoken
- Hugging Face API

### Клиентская часть (frontend):
- HTML5
- CSS3
- JavaScript

### Для парсеров:
- python 3.8 и новее
- Selenium
- Chrome и ChromeDriver
## 📦 Установка и запуск

### Шаг 1: Установите Node.js
1. Перейдите на официальный сайт Node.js
2. Скачайте версию LTS
3. Проверьте установку: откройте командную строку и введите:

```bash
node --version
```
Должна появиться версия Node.js
## Шаг 2: Установите Python (для парсеров)
1. Перейдите на официальный сайт Python
2. Скачайте версию 3.8 или выше
3. При установке ОБЯЗАТЕЛЬНО отметьте галочку "Add Python to PATH"

Проверьте установку:
## Шаг 3: Скачайте проект
1. Скачайте все файлы проекта в одну папку
2. Откройте эту папку в проводнике

## Шаг 4: Установите зависимости
1. Откройте командную строку в папке проекта
2. Введите команду:

```bash
npm install
```

3. Дождитесь завершения установки

## Шаг 5: Установите зависимости Python для парсеров

1. Откройте командную строку в папке проекта
2. Установите необходимые библиотеки:

```bash
pip install selenium requests beautifulsoup4 lxml webdriver-manager schedule
```
3. ОБЯЗАТЕЛЬНО установите Google Chrome браузер с официального сайта

## 🔑 Получение API-ключей
### Ключ для Hugging Face (ИИ)
1. Перейдите на huggingface.com
2. Зарегистрируйте аккаунт
3. После входа перейдите в настройки токенов
4. Нажмите "+ Create new token"
5. Выберите:
```bash
Token name: любое название
Repositories (Поставить галочки): 
Read access to contents of all repos under your personal namespace
Read access to contents of all public gated repos you can access
Make calls to Inference Providers
Manage your Inference Endpoints
```
6. Нажмите на кнопку `Save token`
7. Скопируйте созданный токен (выглядит как hf_xxxxxxxxxxxxxxxxxxx)

### Секретный ключ для JWT
Этот ключ можно придумать самостоятельно. Он используется для шифрования данных авторизации.

## ⚙️ Настройка проекта
### Создайте файл настроек
1. В папке проекта создайте новый файл с именем `.env`
2. Откройте его в любом текстовом редакторе (Блокнот, Notepad++)
3. Добавьте следующие строки:
```bash
PORT=3000
SECRET_KEY=ваш придуманный секретный ключ 
API_KEY=ваш huggingface ключ
```

## Настройка парсера Ozon для сбора товаров
Парсер Ozon автоматически собирает товары с сайта Ozon.ru и сохраняет их в базу данных.

### Настройка конфигурации парсера
1. Найдите в папке проекта файл config.py
2. Откройте его в текстовом редакторе
3. Настройте параметры:

```bash
# Поисковый запрос (что ищем на Ozon)
SEARCH_QUERY = "штаны черные"

# Максимальное количество товаров для парсинга
MAX_PRODUCTS = 30  # или "ALL" для всех товаров

# Количество потоков для парсинга (зависит от скорости интернета)
MAX_WORKERS = 10

# Таймауты (в секундах)
PAGE_LOAD_TIMEOUT = 15    # Время загрузки страницы
ELEMENT_TIMEOUT = 8       # Ожидание элементов
SCROLL_DELAY = 1          # Задержка при скроллинге

# Настройки повторных попыток
MAX_RETRIES = 3           # Попытки при ошибках
RETRY_DELAY = 3           # Задержка между попытками
TASK_TIMEOUT = 15         # Таймаут на задачу
```

Рекомендации:
- MAX_WORKERS лучше ставить 5-15, больше может вызвать сбои
- Для тестирования установите MAX_PRODUCTS = 10
- Для сбора всех товаров установите MAX_PRODUCTS = "ALL"

### Основные команды запуска парсера
#### ПОЛНЫЙ ПАРСИНГ (все товары заново)
```bash
python main.py
```
Что делает:
- Запускает браузер Chrome
- Ищет товары по запросу из config.py
- Собирает все ссылки на товары
- В потоках парсит информацию о каждом товаре
- Сохраняет данные в PRODUCTS_DATA.json

Процесс:
1. Откроется браузер с сайтом Ozon
2. Произойдет поиск по вашему запросу
3. Система автоматически прокрутит страницу, чтобы загрузить все товары
4. Начнется сбор данных о каждом товаре (имя, цена, характеристики, фото)
5. Результаты сохранятся в файл

#### ИНКРЕМЕНТАЛЬНЫЙ ПАРСИНГ (только новые товары)

Что делает:
- Проверяет, какие товары уже есть в базе
- Находит только новые товары, которых еще нет
- Добавляет только новые товары в PRODUCTS_DATA.json
- Не трогает существующие данные

> 📝 **Важно**: Перед запуском инкрементального парсинга нужно хотя бы один раз выполнить полный парсинг.

#### АВТОМАТИЧЕСКИЙ ПЛАНИРОВЩИК

```bash
python scheduler.py
```

**Режимы работы**:
При запуске выберите цифру:
- 1 - Автоматический режим (инкрементальный каждые 90 минут, полный каждые 24 часа)
- 2 - Однократный инкрементальный парсинг
- 3 - Однократный полный парсинг
- 4 - Показать статус
- 0 - Выход

**Для постоянной работы**: Выберите режим 1, и парсер будет работать в фоновом режиме.

### Проверка товаров по артикулам
Если у вас есть список артикулов товаров, вы можете проверить их наличие:
1. Создайте файл Артикулы.txt в папке проекта
2. Добавьте артикулы (по одному на строку), например:

```bash
123456789
987654321
```
3. Запустите проверку:

```bash
python product_checker.py
```
**Результат**: Создастся файл Проверка на наличие товаров.json с информацией о каждом товаре.

### Результаты работы парсера
После работы парсера создаются следующие файлы:
**Основные файлы данных:**
- `PRODUCTS_DATA.json` - полная информация о всех товарах
- `products_urls_dict.json` - словарь ссылок на товары
- `Проверка на наличие товаров.json` - результаты проверки артикулов

**Файлы логов**:
- `parser.log` - логи основного парсера
- `incremental_parser.log` - логи инкрементального парсера
- `advanced_scheduler.log` - логи планировщика
- `product_checker.log` - логи проверки товаров

## 🗃️ Заполнение базы данных товарами
После сбора товаров через парсер Ozon, нужно загрузить их в базу данных.

### Шаг 1: Запустите скрипт для заполнения БД
1. Убедитесь, что файл PRODUCTS_DATA.json существует в папке проекта
2. Запустите скрипт заполнения БД:

```bash
python Запись_БД.py
```

Что делает скрипт:
- Создает все необходимые таблицы в базе данных
- Читает данные из PRODUCTS_DATA.json
- Преобразует данные в нужный формат
- Записывает товары в таблицы:
```bash
Категории
Бренды
Товары
Характеристики_Товаров
```

### Шаг 2: Проверьте результат
1. Откройте BD_test.db
2. Перейдите на вкладку "Данные"
3. Выберите таблицу "Товары" в выпадающем списке
4. Вы должны увидеть список товаров
5. Проверьте другие таблицы: "Характеристики_Товаров", "Категории", "Бренды"

## 🚀 Запуск веб-сервера

### Запуск сервера
1. Откройте командную строку в папке проекта
2. Введите команду:

```bash
node server.js
```

3. Если всё настроено правильно, увидите сообщения:
```bash
DataBase Connected
✅ Server running on port 3000
```

### Открытие сайта
1. Откройте браузер (Chrome, Firefox, Яндекс)
2. В адресной строке введите:
```bash
http://localhost:3000/releasePage.HTML
```


## 📖 Как пользоваться сайтом
1. Первый вход
- Откройте releasePage.HTML в браузере
- Нажмите "Регистрация"
- Заполните:
```bash
Имя (например: "Анна")
Email (например: "anna@mail.ru")
Пароль
Нажмите "Зарегистрироваться"
```
2. Заполнение профиля
- После регистрации войдите в систему
- Перейдите в "Профиль"
- Заполните:
```bash
Рост (например: "165")
Размер (например: "42")
Пол (например: "Женский")
Стиль (например: "Повседневный")
Нажмите "Сохранить изменения"
```
3. Получение рекомендаций
- Перейдите в "Рекомендация"
- Заполните поля (обязательные: пол, размер, рост, стиль)
- Дополнительные поля можно оставить пустыми или указать:
```bash
Цвет (например: "Синий")
Материал (например: "Хлопок")
Сезон (например: "Лето")
```
4. Нажмите "Показать аутфит"
- Ждите 10-30 секунд - ИИ подберет образы

5. Работа с каталогом
- Перейдите в "Каталог"
- Смотрите рекомендованные образы
- Ставьте лайки 👍 или дизлайки 👎
- Нажимайте на сердечко ♥, чтобы добавить в избранное

## 📝 Примечание о Wildberries
В проекте есть файлы для парсинга Wildberries но на данный момент парсер Wildberries нерабочий. Он был использован в разработке для тестирования, но требует обновления под текущую структуру сайта Wildberries.